docker compose up -d

mkdir -r /home/ejfdelgado/server/mysql-phpmyadmin/data

http://localhost:8081/
Username / password: root / root_password
Username / password: user_name / user_password

docker compose down
